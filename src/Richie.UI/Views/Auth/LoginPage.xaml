<Page x:Class="Richie.UI.Views.Auth.LoginPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
      mc:Ignorable="d"
      Title="Log in"
      Foreground="{DynamicResource TextFillColorPrimaryBrush}">
    <Grid>
        <StackPanel Width="360" HorizontalAlignment="Center" VerticalAlignment="Center">
            <ui:TextBlock FontTypography="TitleLarge" HorizontalAlignment="Center" Text="Welcome back" />
            <ui:TextBlock Appearance="Secondary" HorizontalAlignment="Center"
                          Margin="0,0,0,24" Text="Sign in to Richie" />

            <ui:TextBox PlaceholderText="Username"
                        Margin="0,0,0,12"
                        Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}" />
            <ui:PasswordBox x:Name="PasswordBox"
                            PlaceholderText="Password"
                            Margin="0,0,0,12"
                            Password="{Binding Password, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />

            <ui:TextBlock Foreground="#C42B1C"
                          TextWrapping="Wrap"
                          Margin="0,0,0,8"
                          Text="{Binding Error}" />

            <ui:Button Content="Log in"
                       Appearance="Primary"
                       HorizontalAlignment="Stretch"
                       Command="{Binding LoginCommand}"
                       IsDefault="True" />

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,12,0,0">
                <ui:HyperlinkButton Content="Create account" Command="{Binding GoToSignupCommand}" />
                <ui:HyperlinkButton Content="Forgot password?" Command="{Binding GoToForgotPasswordCommand}" />
            </StackPanel>
        </StackPanel>
    </Grid>
</Page>
