<ui:FluentWindow x:Class="Richie.UI.Views.Expenses.ExpenseAnalyticsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        xmlns:lvc="clr-namespace:LiveChartsCore.SkiaSharpView.WPF;assembly=LiveChartsCore.SkiaSharpView.WPF"
        mc:Ignorable="d"
        Title="Expense analytics"
        Height="700" Width="900"
        WindowStartupLocation="CenterOwner"
        ExtendsContentIntoTitleBar="True"
        WindowBackdropType="Mica"
        WindowCornerPreference="Round">
    <ui:FluentWindow.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis" />
    </ui:FluentWindow.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <ui:TitleBar Grid.Row="0" Title="Expense analytics" />

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Padding="24,8,24,16">
            <StackPanel>
                <!-- Category distribution -->
                <Grid Margin="0,0,0,8">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <ui:TextBlock Grid.Column="0" FontTypography="BodyStrong" Text="Category distribution"
                                  VerticalAlignment="Center" TextTrimming="CharacterEllipsis" Margin="0,0,8,0" />
                    <ComboBox Grid.Column="1" HorizontalAlignment="Right" Width="160" ItemsSource="{Binding Periods}"
                              DisplayMemberPath="Text" SelectedValuePath="Value" SelectedValue="{Binding SelectedPeriod}" />
                </Grid>
                <ui:Card Margin="0,0,0,16">
                    <Grid Height="240">
                        <ui:TextBlock Appearance="Secondary" HorizontalAlignment="Center" VerticalAlignment="Center"
                                      Text="No spending in this period." >
                            <ui:TextBlock.Style>
                                <Style TargetType="ui:TextBlock">
                                    <Setter Property="Visibility" Value="Collapsed" />
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding HasCategoryData}" Value="False">
                                            <Setter Property="Visibility" Value="Visible" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </ui:TextBlock.Style>
                        </ui:TextBlock>
                        <lvc:PieChart Series="{Binding CategorySeries}" LegendPosition="Right"
                                      Visibility="{Binding HasCategoryData, Converter={StaticResource BoolToVis}}" />
                    </Grid>
                </ui:Card>

<ui:TextBlock FontTypography="BodyStrong" Text="Monthly spending (last 12 months)" Margin="0,0,0,8" />
                <ui:Card Margin="0,0,0,16">
                    <lvc:CartesianChart Height="220" Series="{Binding MonthlySeries}" XAxes="{Binding MonthlyAxes}" />
                </ui:Card>

                <ui:TextBlock FontTypography="BodyStrong" Text="Yearly spending" Margin="0,0,0,8" />
                <ui:Card Margin="0,0,0,16"
                         Visibility="{Binding HasYearlyData, Converter={StaticResource BoolToVis}}">
                    <lvc:CartesianChart Height="200" Series="{Binding YearlySeries}" XAxes="{Binding YearlyAxes}" />
                </ui:Card>

                <!-- Budgets -->
                <Grid Margin="0,0,0,8">
                    <ui:TextBlock FontTypography="BodyStrong" Text="Monthly budgets (actual vs target)" VerticalAlignment="Center" />
                    <ui:Button Content="Save budgets" Appearance="Primary" HorizontalAlignment="Right" Click="OnSaveBudgets" />
                </Grid>
                <ItemsControl ItemsSource="{Binding BudgetRows}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Grid Margin="0,3">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="2*" />
                                    <ColumnDefinition Width="120" />
                                    <ColumnDefinition Width="120" />
                                    <ColumnDefinition Width="2*" />
                                </Grid.ColumnDefinitions>
                                <ui:TextBlock Grid.Column="0" Text="{Binding Name}" VerticalAlignment="Center" />
                                <ui:TextBox Grid.Column="1" Margin="4,0" PlaceholderText="Limit"
                                            Text="{Binding LimitText, UpdateSourceTrigger=PropertyChanged}" />
                                <ui:TextBlock Grid.Column="2" VerticalAlignment="Center" HorizontalAlignment="Right"
                                              Margin="4,0" Text="{Binding ActualText}" />
                                <ui:TextBlock Grid.Column="3" VerticalAlignment="Center" Margin="8,0,0,0"
                                              Foreground="{Binding StatusBrush}" Text="{Binding StatusText}" />
                            </Grid>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</ui:FluentWindow>
