<Page x:Class="Richie.UI.Views.Pages.AssetDocumentationPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
      xmlns:lvc="clr-namespace:LiveChartsCore.SkiaSharpView.WPF;assembly=LiveChartsCore.SkiaSharpView.WPF"
      xmlns:conv="clr-namespace:Richie.UI.Converters"
      mc:Ignorable="d"
      Title="Asset Documentation">
    <Page.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisibilityConverter" />
        <conv:ProfitLossBrushConverter x:Key="PnlBrush" />
    </Page.Resources>
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Header -->
        <Grid Grid.Row="0" Margin="0,0,0,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <ui:TextBlock Grid.Column="0" FontTypography="TitleLarge" Text="Asset Documentation"
                          Foreground="White" VerticalAlignment="Center" TextTrimming="CharacterEllipsis" Margin="0,0,8,0" />
            <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right">
                <ui:Button Content="Goals" Margin="0,0,8,0" Click="OnOpenGoals" />
                <ui:Button Content="Bulk upload" Margin="0,0,8,0" Click="OnBulkUpload" />
                <ui:Button Content="Add asset" Appearance="Primary" Click="OnAddAsset" />
            </StackPanel>
        </Grid>

        <!-- Summary + allocation -->
        <Grid Grid.Row="1" Margin="0,0,0,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="16" />
                <ColumnDefinition Width="1.4*" />
            </Grid.ColumnDefinitions>

            <ui:Card Grid.Column="0">
                <StackPanel>
                    <ui:TextBlock Appearance="Secondary" Text="Total portfolio value" />
                    <ui:TextBlock FontTypography="TitleLarge" Text="{Binding TotalCurrentValueText}" />
                    <ui:TextBlock Appearance="Secondary" Margin="0,8,0,0"
                                  Text="{Binding TotalInvestedText, StringFormat='Invested: {0}'}" />
                    <ui:TextBlock Appearance="Secondary"
                                  Text="{Binding TotalProfitLossText, StringFormat='Profit / Loss: {0}'}" />
                </StackPanel>
            </ui:Card>

            <ui:Card Grid.Column="2">
                <StackPanel>
                    <ui:TextBlock FontTypography="BodyStrong" Text="Allocation by asset type" Margin="0,0,0,8" />
                    <lvc:PieChart Height="220"
                                  Series="{Binding AllocationSeries}"
                                  LegendPosition="Hidden"
                                  Visibility="{Binding HasAssets, Converter={StaticResource BoolToVisibilityConverter}}" />
                    <ItemsControl ItemsSource="{Binding Allocation}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Grid Margin="0,2">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto" />
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="Auto" />
                                    </Grid.ColumnDefinitions>
                                    <Ellipse Grid.Column="0" Width="10" Height="10" Fill="{Binding Swatch}"
                                             VerticalAlignment="Center" Margin="0,0,8,0" />
                                    <ui:TextBlock Grid.Column="1" Text="{Binding TypeName}" HorizontalAlignment="Left" />
                                    <ui:TextBlock Grid.Column="2" HorizontalAlignment="Right">
                                        <Run Text="{Binding ValueText, Mode=OneWay}" />
                                        <Run Text="{Binding PercentText, Mode=OneWay}" />
                                    </ui:TextBlock>
                                </Grid>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                    <ui:TextBlock Appearance="Secondary" Text="No assets yet."
                                  Visibility="{Binding IsEmpty, Converter={StaticResource BoolToVisibilityConverter}}" />
                </StackPanel>
            </ui:Card>
        </Grid>

        <!-- Asset list -->
        <DataGrid Grid.Row="2" ItemsSource="{Binding Items}" AutoGenerateColumns="False"
                  IsReadOnly="True" CanUserAddRows="False" HeadersVisibility="Column"
                  GridLinesVisibility="Horizontal">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="*" />
                <DataGridTextColumn Header="Type" Binding="{Binding TypeName}" Width="Auto" />
                <DataGridTextColumn Header="Identifier" Binding="{Binding Identifier}" Width="Auto" />
                <DataGridTextColumn Header="Invested" Binding="{Binding InvestedAmount, StringFormat='₹{0:N2}'}" Width="Auto" />
                <DataGridTextColumn Header="Current" Binding="{Binding CurrentValue, StringFormat='₹{0:N2}'}" Width="Auto" />
                <DataGridTextColumn Header="P&amp;L" Binding="{Binding ProfitLoss, StringFormat='₹{0:N2}'}" Width="Auto">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="Foreground" Value="{Binding ProfitLoss, Converter={StaticResource PnlBrush}}" />
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
                <DataGridTextColumn Header="P&amp;L %" Binding="{Binding ProfitLossPercent, StringFormat=&#39;{}{0:+0.0;-0.0;0.0}&#39;}" Width="Auto">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="Foreground" Value="{Binding ProfitLossPercent, Converter={StaticResource PnlBrush}}" />
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
                <DataGridCheckBoxColumn Header="Excl." Binding="{Binding IsExcludedFromPortfolio}" Width="Auto" />
                <DataGridTemplateColumn Header="Actions" Width="Auto">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <ui:Button Content="View" Padding="8,2" Margin="0,0,4,0"
                                           Tag="{Binding Id}" Click="OnViewAsset" />
                                <ui:Button Content="SIP" Padding="8,2" Margin="0,0,4,0"
                                           Tag="{Binding Id}" Click="OnSipAsset" />
                                <ui:Button Content="Docs" Padding="8,2" Margin="0,0,4,0"
                                           Tag="{Binding Id}" Click="OnDocsAsset" />
                                <ui:Button Content="Edit" Padding="8,2" Margin="0,0,4,0"
                                           Tag="{Binding Id}" Click="OnEditAsset" />
                                <ui:Button Content="Delete" Padding="8,2"
                                           Tag="{Binding Id}" Click="OnDeleteAsset" />
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Page>
