<Page x:Class="Richie.UI.Views.Pages.ExportPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
      mc:Ignorable="d"
      Title="Export">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <ui:TextBlock Grid.Row="0" FontTypography="TitleLarge" Text="Export" Foreground="{DynamicResource TextFillColorPrimaryBrush}" Margin="0,0,0,12" />

        <ui:Card Grid.Row="1" VerticalAlignment="Top">
            <StackPanel>
                <ui:TextBlock FontTypography="BodyStrong" Text="Quick export — Full Portfolio" />
                <ui:TextBlock Appearance="Secondary" TextWrapping="Wrap" Margin="0,4,0,12"
                              Text="Exports your full portfolio (assets, expenses, insurance and the vault). You'll be asked whether to include decrypted vault passwords — if so, you'll enter your master password; otherwise passwords are masked. For specific report types or section selection, use the Reports page." />
                <StackPanel Orientation="Horizontal">
                    <ui:Button Content="Export PDF" Appearance="Primary" Margin="0,0,8,0" Click="OnExportPdf" />
                    <ui:Button Content="Export PPTX" Margin="0,0,8,0" Click="OnExportPptx" />
                    <ui:Button Content="Export Excel" Margin="0,0,8,0" Click="OnExportXlsx" />
                    <ui:Button Content="Export CSV" Click="OnExportCsv" />
                </StackPanel>
            </StackPanel>
        </ui:Card>
    </Grid>
</Page>
